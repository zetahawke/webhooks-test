  <div class="section no-pad-bot" id="index-banner">
    <div class="container">
      <table class="table table-hover">
        <thead>
          <tr>
            <th>ID</th>
            <th>data</th>
          </tr>
        </thead>
        <tbody>
          <% @packages.each do |package| %>
            <tr>
              <td><%= package.id %></td>
              <td><%= package.data %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>